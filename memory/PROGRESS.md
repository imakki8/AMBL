# Progress Log

Newest entries on top. Each entry: date, one-line summary, files touched, notes.

---

## 2026-09-04 — Set up repo memory system
- Added `CLAUDE.md` (project overview + memory-system instructions) and `memory/` with `PROGRESS.md`, `DECISIONS.md`, `TASKS.md`.
- Files: `CLAUDE.md`, `memory/PROGRESS.md`, `memory/DECISIONS.md`, `memory/TASKS.md`
- Notes: Repo is a single `index.html` static site (no build step). Prior history (per `git log`): visual redesign inspired by wondermakers.games, real team logos added, roster/schedule/box score data restored, ui-ux-pro-max design skill bundle added. No prior memory system existed.

## (history before this date)
- See `git log` for the raw commit history predating this system:
  - Redesign visual system with wondermakers.games-inspired aesthetic
  - Replace hero logo with clean transparent version
  - Add AMBL hero logo
  - Add real team logos with transparent backgrounds
  - Restore complete index.html with full schedule, roster, and box score data
  - Add ui-ux-pro-max Claude skill bundle for design guidance
